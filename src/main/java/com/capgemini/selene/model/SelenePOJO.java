package com.capgemini.selene.model;

// POJO to be sent via JSON
public class SelenePOJO {
    // temps ? jour / heure ?
    //SeleneData par typee (?) (nom + type + unités + symbole + valeur)
    // Event
}
